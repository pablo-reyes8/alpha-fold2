"""Repository-facing CLI entry points and shared helpers.

The scripts package exposes thin command-line launchers for data preparation,
dataset inspection, model validation, and training. The goal is to provide a
stable operational layer on top of the research modules without duplicating the
core implementation logic.
"""
